<% content_for(:title, t('.title')) %>

<div id="diets">
  <div class="row">
    <% @diets.each do |diet| %>
      <div class="col-md-6">
        <%= render diet %>
      </div>
    <% end %>
  </div>
</div>

<div class="d-grid gap-2">
  <%= link_to t('.action_new'), new_sheet_diet_path(@sheet), data: { controller: "bridge--nav-button", bridge_title: "+ Diet" }, class: 'btn btn-success' %>
</div>

<br>

<%= link_to t('.link_back'), sheets_diet_path, class: 'hide-on-mobile' %>
