<% content_for(:title, @sheet.name) %>

<h1 class="mb-3">
  <%= link_to :back, class: 'd-flex align-items-center gap-2 text-decoration-none text-body' do %>
    <i class="bi bi-arrow-left-short"></i><%= @sheet.name %>
  <% end %>
</h1>

<div class="d-flex flex-column" style="min-height: calc(100vh - 160px);">
  <% if @sheet.workout? %>
    <div class="row">
      <% @workouts.each do |workout| %>
        <div class="col-md-6 mb-4">
          <div class="shadow-sm bg-body-tertiary rounded border">
            <div class="bg-success text-light bg-opacity-20 p-2 rounded-top">
              <h2 class="h6 mb-0 text-truncate">
                <i class="bi bi-activity me-1"></i><%= workout.exercise %>
              </h2>
            </div>

            <div class="p-3">
              <div class="d-flex flex-wrap gap-2">
                <div class="border text-center p-2 rounded flex-grow-1" style="min-width: 45%">
                  <p class="small text-muted mb-0"><i class="bi bi-repeat"></i> <%= t('series') %></p>
                  <p class="fw-bold mb-0"><%= workout.series %></p>
                </div>

                <div class="border text-center p-2 rounded flex-grow-1" style="min-width: 45%">
                  <p class="small text-muted mb-0"><i class="bi bi-arrow-repeat"></i> <%= t('repetitions') %></p>
                  <p class="fw-bold mb-0"><%= workout.repetitions %></p>
                </div>

                <div class="border text-center p-2 rounded flex-grow-1"
                    data-controller="workouts--timer"
                    data-workouts--timer-seconds-value="<%= workout.interval %>">
                  <p class="small text-muted mb-0"><i class="bi bi-stopwatch"></i> <%= t('interval') %></p>
                  <p class="fw-bold mb-0" data-workouts--timer-target="display">
                    <%= format_interval(workout.interval) %>
                  </p>
                  <div class="d-flex justify-content-center gap-3">
                    <button type="button" class="btn btn-sm text-success p-0" data-action="click->workouts--timer#start">
                      <i class="bi bi-play-circle-fill fs-5"></i>
                    </button>
                    <button type="button" class="btn btn-sm text-warning p-0" data-action="click->workouts--timer#pause">
                      <i class="bi bi-pause-circle-fill fs-5"></i>
                    </button>
                    <button type="button" class="btn btn-sm text-danger p-0" data-action="click->workouts--timer#stop">
                      <i class="bi bi-arrow-counterclockwise fs-5"></i>
                    </button>
                  </div>
                </div>

                <div class="border text-center p-2 rounded flex-grow-1" style="min-width: 45%">
                  <p class="small text-muted mb-0"><i class="bi bi-weight"></i> <%= t('charge') %></p>
                  <p class="fw-bold mb-0"><%= t("workouts.form.options_charge.#{workout.charge}", default: workout.charge.to_s.humanize) %></p>
                </div>
              </div>

              <% if workout.video.attached? %>
                <div class="mt-3 text-center">
                  <button class="btn btn-sm border" data-bs-toggle="collapse" data-bs-target="#v-<%= workout.id %>">
                    <i class="bi bi-play-circle"></i> <%= t('show_video') %>
                  </button>
                  <div class="collapse mt-2" id="v-<%= workout.id %>">
                    <div class="ratio ratio-16x9">
                      <%= video_tag url_for(workout.video), controls: true, class: 'rounded' %>
                    </div>
                  </div>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <%= image_tag 'undraw/working_out.svg', alt: '', class: 'img-fluid mt-auto ms-auto', style: 'width: 25rem;' %>
  <% else %>
    <div class="row mb-3">
      <% @diets.each do |diet| %>
        <div class="col-md-6 p-2 mb-2">
          <div class="shadow-sm bg-body-tertiary rounded border">
            <div class="bg-warning text-light bg-opacity-20 p-2 rounded-top">
              <h2 class="h6 mb-0 text-truncate">
                <i class="bi bi-cup-hot me-1"></i><%= diet.meal %>
              </h2>
            </div>

            <div class="px-3 pb-3">
              <p class="mb-3"><%= diet.description %></p>

              <div class="small rounded row g-0 m-0 border">
                <div class="col-6 col-md-3 text-center p-1 border-end">
                  <p class="text-muted mb-0"><i class="bi bi-egg-fried"></i> <%= t('proteins') %></p>
                  <p class="fw-bold mb-0 text-primary"><%= diet.protein_g %>g</p>
                </div>

                <div class="col-6 col-md-3 text-center p-1 border-end">
                  <p class="small text-muted mb-0"><i class="bi bi-basket"></i> <%= t('carbohydrates') %></p>
                  <p class="fw-bold mb-0 text-success"><%= diet.carbohydrate_g %>g</p>
                </div>

                <div class="col-6 col-md-3 text-center p-1 border-end">
                  <p class="small text-muted mb-0"><i class="bi bi-droplet"></i> <%= t('fats') %></p>
                  <p class="fw-bold mb-0 text-warning"><%= diet.fat_g %>g</p>
                </div>

                <div class="col-6 col-md-3 text-center p-1">
                  <p class="small text-muted mb-0"><i class="bi bi-fire"></i> <%= t('calories') %></p>
                  <p class="fw-bold mb-0 text-danger"><%= diet.calories %></p>
                </div>
              </div>
            </div>
          </div>
        </div>
      <% end %>
    </div>

    <%= image_tag 'undraw/cooking.svg', alt: '', class: 'img-fluid mt-auto ms-auto', style: 'width: 25rem;' %>
  <% end %>
</div>
