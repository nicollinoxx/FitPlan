<h1 class='fw-bold'>Avatar</h1>

<div class="d-flex justify-content-center mb-3">
  <% if @user.avatar.attached? %>
    <%= image_tag @user.avatar, class: 'rounded-circle', style: "width: 190px; height: 180px;" %>
  <% else %>
    <%= image_tag 'avatar.png', class: 'rounded-circle', style: "width: 190px; height: 180px;" %>
  <% end %>
</div>

<div class='d-flex flex-row'>
  <%= link_to 'Change Avatar', edit_identity_avatar_path(@user), class: 'btn btn-secondary me-3' %>

  <% if @user.avatar.attached? %>
    <%= button_to t('.action_destroy'), identity_avatar_path(@user), method: :delete, data: { bridge__menu_target: "item", 'turbo-confirm': t('.message_destroy') }, class: 'btn btn-danger' %>
  <% end %>
</div>

<hr>

<%= link_to 'Back to home', home_path %>
