<div id="<%= dom_id workout %>" class="shadow rounded">
  <div class="bg-success bg-opacity-10 p-2 rounded-top d-flex justify-content-between align-items-center">
    <h2 class="h5 fw-bold text-success mb-0 text-truncate">
      <i class="bi bi-activity me-1"></i><%= workout.exercise %>
    </h2>
    <% if action_name != 'show' %>
      <%= link_to sheet_workout_path(@sheet, workout), class: 'btn btn-sm btn-success flex-shrink-0 ms-2' do %>
        <i class="bi bi-eye-fill"></i>
      <% end %>
    <% end %>
  </div>

  <div class="p-3">
    <div class="d-flex flex-wrap gap-2">
      <div class="bg-light text-center p-2 rounded flex-grow-1" style="min-width: 45%">
        <p class="small text-muted mb-0"><i class="bi bi-repeat"></i> <%= t('series') %></p>
        <p class="fw-bold mb-0"><%= workout.series %></p>
      </div>

      <div class="bg-light text-center p-2 rounded flex-grow-1" style="min-width: 45%">
        <p class="small text-muted mb-0"><i class="bi bi-arrow-repeat"></i> <%= t('repetitions') %></p>
        <p class="fw-bold mb-0"><%= workout.repetitions %></p>
      </div>

      <div class="bg-light text-center p-2 rounded flex-grow-1" style="min-width: 45%">
        <p class="small text-muted mb-0"><i class="bi bi-stopwatch"></i> <%= t('interval') %></p>
        <p class="fw-bold mb-0"><%= workout.interval.present? ? workout.interval : '—' %></p>
      </div>

      <div class="bg-light text-center p-2 rounded flex-grow-1" style="min-width: 45%">
        <p class="small text-muted mb-0"><i class="bi bi-weight"></i> <%= t('charge') %></p>
        <p class="fw-bold mb-0"><%= workout.charge.present? ? workout.charge : '—' %></p>
      </div>
    </div>

    <% if workout.video.attached? %>
      <div class="mt-3 text-center">
        <button class="btn btn-sm btn-outline-success" data-bs-toggle="collapse" data-bs-target="#v-<%= workout.id %>">
          <i class="bi bi-play-circle"></i> <%= t('show_video') %>
        </button>
        <div class="collapse mt-2" id="v-<%= workout.id %>">
          <div class="ratio ratio-16x9">
            <%= video_tag url_for(workout.video), controls: true, class: 'rounded' %>
          </div>
        </div>
      </div>
    <% end %>
  </div>
</div>
